## Varriables
=> no need to define its data type
=> but if u want to print all typ of datatype in a single line
then  u cant use '+' sign like java
=>here use ["f"] in {}--> braces only varriable is there
e.g::
print(f"{age} years old {first_Name} {last_Name} is he a genius:: {is_genius}")

## System INPUT
=> use "input" then it automatically take it
e.g:: name = input("whats your name")

## TypeCasting
=> while concatination system treat everyting as "String" so use type casting 
e.g:: int(variable), str(), bool(), float()
number= 12
total = number + 2 -----> Wrong
total = int(number) + 2 ------> Correct

## Strings
=> in python it written like "**str**"

### Str Functions 
lower() ---> lowercase

upper()----> Upper case 

find()---> inside write what u want to search and if its present then it will return that cha position(index no.)

replace(old,new)----> 2 parameter --> 1st give what u want to replace , 2nd what thing u will want to place

(what want to search **in** varriable)===> return boolean this thing present or not

### Arithmatic Operation
=> **print(5/2)** ---> 2.5 if u want here only before decimal print then

e.g:: print(5//2)-----> ans::2

=> **print(5 ** 2)**----> 5 to power 2